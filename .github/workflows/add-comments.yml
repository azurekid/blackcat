# name: Issue Comment
# on:
#     issues:
#         types: [ labeled, opened ]
#     issue_comment:
#         types: [ created ]
# jobs:
#     comment:
#         runs-on: ubuntu-latest
#         permissions:
#             issues: write
#         steps:
#             - name: Debug context
#               run: |
#                 echo "Event name: ${{ github.event_name }}"
#                 echo "Action: ${{ github.event.action }}"
#                 echo "Issue number: ${{ github.event.issue.number }}"
#                 echo "Repository: ${{ github.repository }}"
#                 echo "Sender: ${{ github.event.sender.login }}"
            
#             - name: Add initial comment
#               uses: actions/github-script@v6
#               with:
#                 github-token: ${{ secrets.GITHUB_TOKEN }}
#                 script: |
#                     github.rest.issues.createComment({
#                         issue_number: context.issue.number,
#                         owner: context.repo.owner,
#                         repo: context.repo.repo,
#                         body: 'Thank you for submitting an Issue to the project BlackCat GitHub repo!
#                         You should expect an initial response to your Issue within 48 hours.'
#                     })
name: Auto Comment
on: [issues]
jobs:
  run:
    runs-on: ubuntu-latest
    permissions:
        issues: write
    steps:
      - uses: wow-actions/auto-comment@v1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          issuesOpenedReactions: 'eyes'
          issuesOpenedComment: |
            👋 @{{ author }}
            Thank you for raising an issue on Project BlackCat.
            We appreciate your contribution and will review it as soon as possible.
            
            If you have any additional information or context to provide, please feel free to add it in the comments.
            
            We are here to help!
